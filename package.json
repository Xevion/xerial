{
  "name": "xerial",
  "module": "src/parser/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prepare": "panda codegen",
    "codegen": "panda codegen",
    "check": "panda codegen && svelte-check --tsconfig ./tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@pandacss/dev": "^1.11.3",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@types/bun": "latest",
    "svelte": "^5.56.3",
    "svelte-check": "^4.6.0",
    "vite": "^8.0.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
